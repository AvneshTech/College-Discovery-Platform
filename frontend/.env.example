# Base origin of the deployed backend API (must match the backend's CLIENT_URL/CORS).
# Used by lib/apiClient.ts, the upload client, and the realtime socket connection.
NEXT_PUBLIC_API_URL=http://localhost:5000
